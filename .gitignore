.vendor/
.venv/
__pycache__/
*.pyc
.DS_Store
.playwright-cli/
*.tsbuildinfo

# The full estate is deterministic and is regenerated locally. Keeping the
# 565 MiB snapshot out of Git avoids repository/LFS coupling; the browser pilot
# carries only its small scenario-specific data pack under web/public/data.
parquet/
